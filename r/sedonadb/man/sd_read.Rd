% Generated by roxygen2: do not edit by hand
\name{sd_read}
\alias{sd_read}
\alias{sd_ctx_read}
\title{Read one or more files into a DataFrame}
\usage{
sd_read(file_or_files, options = list(), format = NULL, partitioning = NULL)

sd_ctx_read(
  ctx,
  file_or_files,
  options = list(),
  format = NULL,
  partitioning = NULL
)
}
\arguments{
\item{file_or_files}{One or more paths or URIs.}

\item{options}{A named list of scalar reader or object-store options.}

\item{format}{An optional file format name such as \code{"parquet"}, \code{"csv"},
or \code{"json"}. By default the format is inferred from the path extension.}

\item{partitioning}{Optional character vector of hive-style partition column
names. \code{NULL} auto-discovers partitions; \code{character()} disables discovery.}

\item{ctx}{A SedonaDB context.}
}
\value{
A sedonadb_dataframe
}
\description{
Resolves the reader from \code{format}, or from the file extension when \code{format}
is \code{NULL}. The query is executed lazily when results are requested.
}
\examples{
path <- system.file("files/natural-earth_cities_geo.parquet", package = "sedonadb")
sd_read(path) |> head(5) |> sd_preview()
}
